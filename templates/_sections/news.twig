{#
 # BASE TEMPLATE: NEWS SECTION
 #}
{% extends "_layout/default" %}

{% block content %}
	{# Use the _entryTypes partial for this entry type, otherwise show "missing entry type template" error message #}
	{% include [
			("_entryTypes/" ~ entry.type), 
			"_entryTypes/_missing"
		]
		with {'entry' : entry}
	%}
{% endblock %}
