{% if buttons | length %}
<p>
	{% for button in buttons %}
		{% if button.btnLink.getLink() %}
			{% include "_fields/button" with {'button': button.btnLink, 'color': button.btnStyle, 'class': button.arrow == 1 ? 'btn-arrow m-2' : 'm-2' } %}
		{% endif %}
	{% endfor %}
</p>
{% endif %}