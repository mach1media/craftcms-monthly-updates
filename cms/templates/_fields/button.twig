{% if button | length %}
	{% set classes = ['btn'] %}
	{% set classes = color is defined ? classes|merge([color]) : classes|merge(['btn-primary']) %}
	{% if class is defined and class != '' %}
		{% set classes = classes|merge([class]) %}
	{% endif %}
	{{ button.getLink({'class':classes|join(' ')}) }}
{% endif %}