{% if button | length %}
	{% set classes = ['btn'] %}
	{% if color is defined and color != '' %}
		{% set classes = classes|merge([color]) %}
	{% endif %}
	{% if style is defined and style != '' %}
		{% set classes = classes|merge([style]) %}
	{% endif %}
	{% if class is defined and class != '' %}
		{% set classes = classes|merge([class]) %}
	{% endif %}
	{% if download is defined and download == 1 %}
		{{ button.getLink({
			'class':classes|join(' '),
			'download':'download'
		}) }}
	{% else %}
		{{ button.getLink({'class':classes|join(' ')}) }}
	{% endif %}
{% endif %}