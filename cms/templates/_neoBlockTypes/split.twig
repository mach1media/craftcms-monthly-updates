{##
 # PAGE HEADER: SPLIT
 #}
<section class="c-pagehead c-pagehead--split">
	<div class="container">
		<div class="row">
			<div class="col-12 col-md-6 order-md-2 mb-4 mb-md-0">
				{% if block.image is defined and block.image | length %}
				{% set img    = block.image.one %}
				{% set imgSrc = img.url('responsiveMedium') %}
				{% set imgAlt = img.alt ?: img.caption %}
				<p>
					<img class="img-fluid" src="{{ imgSrc }}" alt="{{ imgAlt }}" />
				</p>
				{% endif %}
			</div>
		
			<div class="col-12 col-md-6 order-md-1">
				{% if block.heading is defined and block.heading != '' %}<h1 class="h3 mb-4">{{ block.heading | raw }}</h1>{% endif %}
				{{ block.description }}
			</div>
		</div>
	</div>
</section>
