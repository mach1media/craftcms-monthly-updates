{##
 # NEO BLOCK TYPE: RESOURCE CARDS
 #}
<section id="{{ sectionId }}" class="c-block{% if sectionStyleClasses is defined and sectionStyleClasses != '' %} {{ sectionStyleClasses }}{% endif %}">
	<div class="container">
		<div class="row justify-content-center">
			<div class="col-12 col-lg-10 col-xl-9 {{ block.textColor }}">
				{% if block.preheading != '' 
					or block.headingBlock | length 
					or block.description != ''
				%}
				<div class="text-center mb-4">
					{% if block.preheading is defined and block.preheading != '' %}
					<p class="u-preheading">{{ block.preheading }}</p>
					{% endif %}
					
					{% include "_fields/headingBlock" with {'heading': block.headingBlock} %}
					
					{% if block.description is defined and block.description != '' %}
					<div class="c-wysiwyg">
						{% apply enkode_emails %}
							{{ block.description }}
						{% endapply %}
					</div>
					{% endif %}
				</div>
				{% endif %}
			</div>

			<div class="col-12">
				<div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 justify-content-center">
					{% for resource in block.resourceCards %}
					{% set asset = resource.resource %}
					<div class="col mb-4">
						<article class="c-card card bg-white h-100">
							<div class="card-body d-flex flex-column justify-content-between">
								<div class="text-center px-4">
									{% if resource.thumbnail | length %}
										{% set img    = resource.thumbnail.one %}
										{% set imgSrc = img.format != 'svg' ? img.url('resourceCard') : img.url %}
										{% set imgAlt = img.alt ?: resource.title %}
									{% else %}
										{% set imgSrc = '/dist/img/placeholder-resource-' ~ cycle([1,2,3], loop.index) ~ '.jpg' %}
										{% set imgAlt = 'placeholder' %}
									{% endif %}
									<div class="mb-3">
										<img class="img-fluid mx-auto" src="{{ imgSrc }}" alt="{{ imgAlt }}" style="max-height:240px;width:auto;">
									</div>
									<p>{{ resource.cardTitle ?: asset.title }}</p>
								</div>
								
								<div class="pt-4 text-center">
									<a class="btn btn-primary btn-arrow" href="{{ asset.getIntrinsicUrl }}" target="{{ asset.target }}">{{ resource.buttonLabel ?: 'Download' }}</a>
								</div>
							</div>
						</article>
					</div>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
</section>